test.skip('it starts the compiler', async () => {
  expect(false).toBe(true);
});

test.skip('it passes the configuration to the compiler', async () => {
  expect(false).toBe(true);
});

test.skip('it passes the nock cli param to the compiler', async () => {
  expect(false).toBe(true);
});

test.skip('it passes the record cli param to the compiler', async () => {
  expect(false).toBe(true);
});

test.skip('it passes the nockPath cli param to the compiler', async () => {
  expect(false).toBe(true);
});
